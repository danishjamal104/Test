# Git Commands

*git init
*git add <filename>
*git status
*git log
*git commit -m "my new commit" 
*git diff <filename>
*git branch <branchname>
*git checkout <branchname>
*git checkout -b <branchname>
*git fetch
*git push
*git pull
*git branch
